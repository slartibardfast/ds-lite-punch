[package]
name = "ds-lite-punch"
version = "0.3.4"
edition = "2021"
description = "CGNAT-aware UDP relay for the Virgin Media ds-lite line: keeps the AFTR mapping alive with STUN keepalives and forwards inbound UDP to a br-lan target, preserving the peer source."

[[bin]]
name = "ds-lite-punch"
path = "src/main.rs"

[dependencies]
tokio = { version = "1", features = ["rt", "rt-multi-thread", "net", "io-util", "time", "macros", "sync", "signal"] }
libc = "0.2"

# Register the kani cfg so rustc stays quiet about the proof modules when Kani is not running.
[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
