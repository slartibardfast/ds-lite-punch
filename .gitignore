/target/

# The CLI authoring tool builds for the host, so its target directory sits
# under tools/ and the anchored rule above does not cover it.
/tools/argdoc/target/

# A harness's scratch: the review tool writes .qwen/tmp/ here, and an untracked
# tree like that is how it got swept toward a commit once already.
/.qwen/
